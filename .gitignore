# Build artifacts
/proxy.exe
/inference-adapter.exe
*.exe

# Local LFM models (large; download separately)
models/

# Local state / audit DB
*.db
*.db-journal
*.db-wal
state/

# Runtime logs (proxy / llama-server)
*.log

# Local classifier test log (kept locally, not version-controlled)
proxy-server/docs/lfm2.5-classification-test.txt

# Admin UI (Next.js) build / deps / local env
proxy-server/web/node_modules/
proxy-server/web/.next/
proxy-server/web/.next-mock/
proxy-server/web/out/
proxy-server/web/.env.local
proxy-server/web/next-env.d.ts
proxy-server/web/tsconfig.tsbuildinfo

.env
wandb/
